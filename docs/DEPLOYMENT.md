# DEPLOYMENT.md

## Template repository

This repository has no application runtime deployment. Distribution is through GitHub and raw file access.

## Derived projects

Replace this file with the project's real deployment facts:

- deployment targets and environments,
- required environment variables/secrets,
- build/release commands,
- smoke tests,
- rollback procedure,
- operational dependencies,
- monitoring/logging entry points.

Do not store secrets here. Record mechanisms and variable names only.
